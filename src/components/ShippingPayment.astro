---
const paymentMethods = [
	{ name: "Efectivo", icon: "cash" },
	{ name: "Transferencia", icon: "transfer" },
	{ name: "Tarjetas", icon: "card" },
];
---

<section class="shipping-payment section">
	<div class="container">
		<h2 class="section-title">Envío y Pago</h2>
		<div class="section-divider"></div>

		<div class="content-grid">
			<div class="info-card">
				<div class="card-icon shipping-icon">
					<svg
						width="32"
						height="32"
						viewBox="0 0 24 24"
						fill="none"
						stroke="currentColor"
						stroke-width="2"
					>
						<rect x="1" y="3" width="15" height="13"></rect>
						<polygon points="16 8 20 8 23 11 23 16 16 16 16 8"
						></polygon>
						<circle cx="5.5" cy="18.5" r="2.5"></circle>
						<circle cx="18.5" cy="18.5" r="2.5"></circle>
					</svg>
				</div>
				<h3 class="card-title">Envío a Domicilio</h3>
				<p class="card-description">
					Entregamos directamente en tu negocio con rutas eficientes y
					puntuales. Consulta cobertura en tu zona.
				</p>
			</div>

			<div class="info-card">
				<div class="card-icon payment-icon">
					<svg
						width="32"
						height="32"
						viewBox="0 0 24 24"
						fill="none"
						stroke="currentColor"
						stroke-width="2"
					>
						<rect x="2" y="5" width="20" height="14" rx="2"></rect>
						<line x1="2" y1="10" x2="22" y2="10"></line>
					</svg>
				</div>
				<h3 class="card-title">Métodos de Pago</h3>
				<div class="payment-methods">
					{
						paymentMethods.map((method) => (
							<div class="payment-badge">
								<span>{method.name}</span>
							</div>
						))
					}
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.shipping-payment {
		background-color: var(--color-gray-50);
	}

	.content-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 2rem;
		max-width: 900px;
		margin: 0 auto;
	}

	.info-card {
		background-color: var(--color-white);
		padding: 2.5rem 2rem;
		border-radius: 1rem;
		text-align: center;
		border: 1px solid var(--color-gray-100);
		transition:
			transform 0.3s ease,
			box-shadow 0.3s ease;
	}

	.info-card:hover {
		transform: translateY(-4px);
		box-shadow: var(--shadow-lg);
	}

	.card-icon {
		width: 4.5rem;
		height: 4.5rem;
		margin: 0 auto 1.5rem;
		border-radius: 1rem;
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--color-white);
	}

	.shipping-icon {
		background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
	}

	.payment-icon {
		background: linear-gradient(135deg, #10b981 0%, #059669 100%);
	}

	.card-title {
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--color-gray-900);
		margin-bottom: 1rem;
	}

	.card-description {
		font-size: 1rem;
		color: var(--color-gray-600);
		line-height: 1.6;
	}

	.payment-methods {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		justify-content: center;
		margin-top: 1rem;
	}

	.payment-badge {
		background-color: var(--color-gray-100);
		padding: 0.5rem 1rem;
		border-radius: 9999px;
		font-size: 0.875rem;
		font-weight: 600;
		color: var(--color-gray-700);
	}

	@media (max-width: 768px) {
		.content-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
